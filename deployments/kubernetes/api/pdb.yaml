apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: api-pdb
  namespace: urlshortener
  labels:
    app: api
    component: availability
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: api
